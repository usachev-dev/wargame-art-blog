'use strict';

import ImgZoomable from '../img-zoomable';

describe('ImgZoomable View', function() {

  beforeEach(() => {
    this.imgZoomable = new ImgZoomable();
  });

  it('Should run a few assertions', () => {
    expect(this.imgZoomable);
  });

});
