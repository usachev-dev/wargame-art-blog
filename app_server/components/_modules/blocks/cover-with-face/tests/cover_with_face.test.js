'use strict';

import CoverWithFace from '../cover-with-face';

describe('CoverWithFace View', function() {

  beforeEach(() => {
    this.coverWithFace = new CoverWithFace();
  });

  it('Should run a few assertions', () => {
    expect(this.coverWithFace);
  });

});
