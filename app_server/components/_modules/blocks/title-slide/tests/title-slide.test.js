'use strict';

import TitleSlide from '../title-slide';

describe('TitleSlide View', function() {

  beforeEach(() => {
    this.titleSlide = new TitleSlide();
  });

  it('Should run a few assertions', () => {
    expect(this.titleSlide);
  });

});
