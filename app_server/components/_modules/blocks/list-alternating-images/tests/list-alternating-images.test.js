'use strict';

import ListAlternatingImages from '../list-alternating-images';

describe('ListAlternatingImages View', function() {

  beforeEach(() => {
    this.listAlternatingImages = new ListAlternatingImages();
  });

  it('Should run a few assertions', () => {
    expect(this.listAlternatingImages);
  });

});
