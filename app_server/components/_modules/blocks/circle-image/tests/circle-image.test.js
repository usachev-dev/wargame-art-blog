'use strict';

import CircleImage from '../circle-image';

describe('CircleImage View', function() {

  beforeEach(() => {
    this.circleImage = new CircleImage();
  });

  it('Should run a few assertions', () => {
    expect(this.circleImage);
  });

});
