'use strict';

import ImageWithText from '../image-with-text';

describe('ImageWithText View', function() {

  beforeEach(() => {
    this.imageWithText = new ImageWithText();
  });

  it('Should run a few assertions', () => {
    expect(this.imageWithText);
  });

});
