'use strict';

import ImageWithCaption from '../image-with-caption';

describe('ImageWithCaption View', function() {

  beforeEach(() => {
    this.imageWithCaption = new ImageWithCaption();
  });

  it('Should run a few assertions', () => {
    expect(this.imageWithCaption);
  });

});
