'use strict';

import List from '../list';

describe('List View', function() {

  beforeEach(() => {
    this.list = new List();
  });

  it('Should run a few assertions', () => {
    expect(this.list);
  });

});
