'use strict';

import ChapterTitle from '../chapter-title';

describe('ChapterTitle View', function() {

  beforeEach(() => {
    this.chapterTitle = new ChapterTitle();
  });

  it('Should run a few assertions', () => {
    expect(this.chapterTitle);
  });

});
