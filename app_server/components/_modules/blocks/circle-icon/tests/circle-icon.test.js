'use strict';

import CircleIcon from '../circle-icon';

describe('CircleIcon View', function() {

  beforeEach(() => {
    this.circleIcon = new CircleIcon();
  });

  it('Should run a few assertions', () => {
    expect(this.circleIcon);
  });

});
